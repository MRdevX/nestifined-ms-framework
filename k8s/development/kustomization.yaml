bases:
  - ../base

namespace: nestifined-development

resources:
  - namespace.yaml

patchesStrategicMerge:
  - deployment.yaml

configMapGenerator:
  - name: nestifined-ms-framework-config
    literals:
      - NODE_ENV=development
